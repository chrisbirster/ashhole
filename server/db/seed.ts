export async function seedPublicData() {
  // Intentionally empty. Historical ASHHOLE data comes from
  // `npm run import:legacy -- /path/to/ashhole90`, and future years are
  // maintained through /admin. Keeping this function preserves the existing
  // db:seed command without compiling annual golf data into the application.
}
